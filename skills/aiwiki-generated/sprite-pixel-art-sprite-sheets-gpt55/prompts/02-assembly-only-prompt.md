# Assembly-Only Prompt

```text
Create a single sprite sheet from the uploaded approved frames.

This is an assembly task only.

Requirements:
- use the exact uploaded frame artwork
- do not redraw, repaint, enhance, restyle, reinterpret, or modify the subject
- preserve exact character identity, colors, palette, outline thickness, and pixel-art style
- same cell width and same cell height for all frames
- align all frames to bottom center
- preserve one shared ground baseline
- transparent PNG background
- no checkerboard background
- no props, scenery, UI, labels, frame numbers, or extra frames

Output:
- a single transparent PNG sprite sheet
- [state row/column layout explicitly]
```
